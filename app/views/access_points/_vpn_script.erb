#!/bin/sh

up() {
  # TO DO: openvpn scripts
}


down() {
  # TO DO: openvpn scripts
}

# Syntax:  <script> tap_dev tap_mtu link_mtu ifconfig_local_ip ifconfig_netmask [ init | restart | ??? ]
eval "last=\${$#}"
case "$last" in
  init)
    up
    ;;
  restart)
    down ; up
    ;;
  *)
    down
    ;;
esac
