Description:
    Creates a migration to add the mark table used by ActsAsMarkableOnChange.
    The table name cannot be changed (for now...). Edit the plugin if you
    need different table names.

Example:
    ./script/generate aamoc_migration

    Assuming this is run at 09:00:15h on 12 September, 2008, this will create the
    CreateMarks migration in:

        db/migrate/20080912090015_create_marks.rb
